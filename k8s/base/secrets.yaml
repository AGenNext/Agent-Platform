# ── IMPORTANT ────────────────────────────────────────────────────────────────
# Do NOT commit real secrets. Replace placeholders before deploying.
# Use: echo -n 'value' | base64
# Or seal with: kubeseal --format=yaml < secrets.yaml > secrets.sealed.yaml
# ─────────────────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: platform-secrets
  namespace: realgraph
type: Opaque
data:
  # echo -n 'root' | base64
  SURREALDB_USERNAME: cm9vdA==
  # echo -n 'changeme' | base64
  SURREALDB_PASSWORD: Y2hhbmdlbWU=
  # echo -n 'minioadmin' | base64
  MINIO_ROOT_USER: bWluaW9hZG1pbg==
  # echo -n 'changeme' | base64
  MINIO_ROOT_PASSWORD: Y2hhbmdlbWU=
  # echo -n '' | base64  (set if using Anthropic)
  ANTHROPIC_API_KEY: ""
  # echo -n '' | base64  (set if using OpenAI)
  OPENAI_API_KEY: ""
  # echo -n '' | base64  (set if using Cohere)
  COHERE_API_KEY: ""
